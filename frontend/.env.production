# NOTE: This is just a placeholder template. Override at deploy time
# (e.g. Vercel project env vars, Netlify _variables, etc.) with the
# real public HTTPS URL of your Summarix backend deployment.
#
# Vite embeds VITE_* variables into the static bundle at BUILD time,
# so make sure you set this BEFORE running `npm run build`.
VITE_API_BASE_URL=https://your-summarix-backend.example.com
